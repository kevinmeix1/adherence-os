# Optional public base URL for Open Graph and Twitter image links.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional. Leave blank for the recommended deterministic public-demo mode.
# Do not expose the unauthenticated care-plan route with an unrestricted paid key.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.5
